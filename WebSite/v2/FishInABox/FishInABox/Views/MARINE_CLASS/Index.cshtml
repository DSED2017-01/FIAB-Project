@model IEnumerable<FishInABox.Models.DataModel.MARINE_CLASS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Cam's Marine Classification Text
        </th>
        <th>
            MPI Marine Classification
        </th>
        <th>
            <!-- @Html.DisplayNameFor(model => model.FLAG)   -->
            
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TEXT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SCHEDULE4)
        </td>
        <td>
            <!-- @Html.DisplayFor(modelItem => item.FLAG)  -->
            
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PK }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID_PK }) |
            @if (item.FLAG == false)
            {
                @Html.ActionLink("Delete", "Delete", new { id = item.ID_PK })
            }
            
        </td>
    </tr>
}

</table>
