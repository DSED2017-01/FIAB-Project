@model IEnumerable<FishInABox.Models.DataModel.MARINE_CLASS>

@{
    ViewBag.Title = "Index";
}

<h2>Marine Classification Maintenance Menu</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Cam's Terminology
        </th>
        <th>
            MPI Terminology
        </th>
        <th>
            <!-- @Html.DisplayNameFor(model => model.FLAG)   -->
            
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TEXT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SCHEDULE4)
        </td>
        <td>
            <!-- @Html.DisplayFor(modelItem => item.FLAG)  -->
            
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PK }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID_PK }) |
            @if (item.FLAG == false)
            {
                @Html.ActionLink("Delete", "Delete", new { id = item.ID_PK })
            }
            
        </td>
    </tr>
}

</table>
<!-- @Html.ActionLink("Return to System Menu", "Index", "System")  -->
<input type="button" class='smallerClickButton'
       value="&laquo; Return to System Menu" onclick="location.href='@Url.Action("Index", "System")'" />


